#include <stdio.h>
#include <stdlib.h>
int main()
{
  // double a = 0.0, b = 0.0, c = 1.0, d = 0.0;

  // d = 1.2;
  // // a = a - b / (c-d) * c;
  // a = d + b / c * 2 - 2;
  // int q = 0, s = 0, r = 0;
  // q = s + r;
  // printf("%f", a);
  char * w = malloc(sizeof w * 255);
  // w = "asdassss";
  // w = "qwe";
  scanf("%s", w);
  printf("%s", w);
}
